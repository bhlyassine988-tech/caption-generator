import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Social Media Caption Generator - AI-Powered Captions',
  description: 'Generate engaging, contextual captions for social media posts in seconds. AI-powered creativity for Instagram, Twitter, LinkedIn, and TikTok.',
  keywords: 'social media captions, AI caption generator, Instagram captions, Twitter captions, LinkedIn posts, TikTok captions',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster 
          position="bottom-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
          }}
        />
      </body>
    </html>
  )
}
